name: create-pandora-react-app
root: ~/.projects_root/create-pandora-react-app

windows:
  - server:
      layout: main-vertical
      panes:
        - load_nvm &&
          nvm use 8.9.4 &&
          npm start
  - editor:
      layout: main-vertical
      panes:
        - vim
  - zsh:
      layout: main-vertical
      panes:
        - clear
