# /Users/tcraig/Dotfiles/tmuxinator/projects/orcus.yml

name: pandora-harmony
root: ~/.projects_root/pandora-harmony

windows:
  - server:
      layout: main-vertical
      panes:
        - load_nvm &&
          nvm use 8.9.4 &&
          npm start

  - editor: vim
  - zsh:
      layout: main-vertical
      panes:
        - clear
