name: Ceres
root: ~/.projects_root/ceres


windows:
  - Notes: cd ${DROPBOX_ROOT}/CeresNotes && vim -c ":set spell " tasks.md
  - Servers:
      layout: even-vertical
      panes:
        - cd ceres_works
        - cd osiris
        - cd avian
  - Vim: vim
