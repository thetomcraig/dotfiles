name: Silo
root: ~/.projects_root/ceres/silo

windows:
  - server:
      layout: main-vertical
      panes:
        - tmux send-keys "docker-compose up"
  - editor: vim
  - zsh:
      layout: main-vertical
      panes:
        - clear
